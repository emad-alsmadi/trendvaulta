# Devin Project Rules - TrendVaulta

## Project Overview
TrendVaulta is a monorepo retail e-commerce platform for beauty, fashion, and lifestyle products. It features user authentication, product and brand browsing, cart and Stripe checkout, orders, wishlist, reviews/ratings, coupons/offers, and an admin dashboard.

## Tech Stack
- **Storefront**: Next.js 16 with App Router (`apps/website`, port 3001)
- **Admin dashboard**: Vite + React 19 with React Router (`apps/dashboard`, port 3002)
- **Backend Framework**: Express.js with Node.js (`apps/api`, port 3000)
- **Database**: MongoDB with Mongoose
- **Styling**: Tailwind CSS
- **State Management**: Zustand (cartStore, authStore)
- **Data Fetching**: React Query (@tanstack/react-query)
- **Forms**: React Hook Form
- **Icons**: Lucide React
- **Animations**: Framer Motion
- **Payment**: Stripe
- **Package Manager**: npm workspaces (root `package.json`, single root lockfile)

## Monorepo Structure
```
trendvaulta/
├── apps/
│   ├── api/          # Express.js backend (port 3000)
│   ├── website/      # Next.js storefront (port 3001)
│   └── dashboard/    # Vite admin dashboard (port 3002)
├── packages/         # @trendvaulta/types, @trendvaulta/api-client
└── package.json      # Root npm workspaces config
```

## Frontend File Structure (apps/website/src/)
- `app/` - Next.js App Router pages
- `components/` - Reusable UI components
- `components/ui/` - Base UI components (Button, Input, etc.)
- `components/navigation/` - Navigation components
- `lib/` - Utilities, API clients, types
- `hooks/` - Custom React hooks organized by domain
- `types/` - TypeScript type definitions

## Backend File Structure (apps/api/)
- `controllers/` - Route controllers
- `models/` - Mongoose models
- `routes/` - Express route definitions
- `middlewares/` - Custom middleware
- `services/` - External service integrations (Stripe)
- `utils/` - Helper functions

## Code Style
- Use functional components with hooks (frontend)
- Use async/await for async operations
- Keep components small and focused
- Use TypeScript for type safety
- Follow existing naming conventions (camelCase for variables, PascalCase for components)
- Use meaningful variable and function names

## API Integration
- Frontend uses `api.ts` in `lib/` for all API calls
- Backend uses Express.js with middleware for authentication
- Store auth tokens in cookies (authCookies)
- Use React Query for data fetching and caching
- Handle errors gracefully with user-friendly messages
- Follow RESTful API patterns

## Styling Guidelines
- Use Tailwind CSS for all styling
- Follow the existing design system (purple-indigo-cyan gradient theme)
- Ensure responsive design (mobile-first approach)
- Use glassmorphism effects (backdrop-blur, transparency)
- Maintain consistent spacing and typography
- Use Framer Motion for smooth animations

## Key Features
- User authentication with JWT
- Product and brand browsing, search, filters
- Wishlist management
- Reviews and ratings system
- Shopping cart functionality
- Stripe payment integration
- Order management
- Admin dashboard (products, brands, orders, users, coupons, offers, reviews, stats)

## Git Workflow
- Write clear, descriptive commit messages
- Use conventional commit format (feat:, fix:, refactor:)
- Keep commits atomic and focused
- Test changes before committing
